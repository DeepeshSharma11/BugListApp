import React from 'react';

// Disabling the WebGL particle background to reduce GPU usage to 0
// and prevent website lag/hanging on low-end devices.
export default function ParticleBackground() {
  return null;
}
